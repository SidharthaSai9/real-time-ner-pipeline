input {
  kafka {
    bootstrap_servers => "kafka:9092"
    topics => ["ner-counts"]
    codec => "json"
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "news-data"
  }
}
